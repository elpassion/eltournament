import { saneIndex } from 'eltournament/helpers/sane-index';
import { module, test } from 'qunit';

module('Unit | Helper | sane index');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = saneIndex([42]);
  assert.ok(result);
});
